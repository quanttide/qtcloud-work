# 任务页

一次执行实例。左边对话，右边状态面板。

- **页头**：任务切换、`＋ 起一件任务`（`task --new`）、`任务列表`（`task --list`）
- **左**：[对话](../views/chat.md)，含流程草案卡
- **右**：[状态面板](../views/status-panel.md)

数据：任务的 `log`／`gates`／`products`；流程侧只取步骤数与步骤名。

同目录的 `task.html`：这一屏单独打开的样子（自带样式，可直接在浏览器里看）。
