# 侧栏

三项，各两字，切页用，不装别的。

| 项 | 去哪个屏 | 对应命令行 |
|---|---|---|
| 任务 | 任务页 | `task …` |
| 流程 | 流程页 | `workflow …` |
| 设置 | 设置页 | `--root`／`--data`／`--workflows`／`health` |

导航里写「流程」，命令行里这一侧叫「工作流」——两边指同一件事。

同目录的 `sidebar.html`：这一块的静态样子。
