name: qtcloud_work_studio
description: "Studio of QuantTide Knowledge Work Cloud"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # 领域模型抽到工具箱之后，这里是它的第一个消费者
  quanttide_work: ^0.1.0-alpha.10
  yaml: ^3.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
