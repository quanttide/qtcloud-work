# help

```bash
qtcloud-work help [<话题>]
```

导览：按用途把命令分组列出来（工作区 / 工作流 / 工单 / 其他），末尾给四处全局位置与看细则的办法。给了话题（命令名）就只说那一条：一句话要点、细则去哪看、契约在哪篇。

认不出的名字：退出码 1，提示 `qtcloud-work help` 看全部。与 `--help` 的分工：`--help` 是某个命令的选项全集，`help` 是这份导览。
